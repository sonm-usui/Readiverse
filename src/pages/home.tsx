import { Box } from 'native-base';
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Home = ({navigation}: any) => {
    return (
       <>
       <Text>hello</Text>
       {/* <Box position="absolute" bottom={0}>
                    <NavigateButton navigation={navigation}/>
                </Box> */}
       </> 
    );
}

export default Home;
